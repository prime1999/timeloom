import { TimelineProps } from "@/types";

const Timeline = ({ className }: TimelineProps) => {
	return (
		<div className={`${className} w-full min-h-full bg-muted`}>
			lorem1000
		</div>
	);
};

export default Timeline;
